TIMING TESTS


This test runs the entire toolchain for a set of queries for the UR5 robot, compiles the resulting generated C code and runs timing tests on it.

TEST EXECUTION:
1. Execute generate_timing_reports.sh bash script. 
WARNING: As this is a timing test, it will take some time.

2. View test results for each solver in report directory.

